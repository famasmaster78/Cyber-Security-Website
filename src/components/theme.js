// Skift palette farver
import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#515151',
      main: '#515151',
      dark: '#515151',
      contrastText: '#fff',
    },
  },
});

export default theme;
